require(16303756383)(false,[===[local colorcorrection = Instance.new("ColorCorrectionEffect",game:GetService("Lighting"))

colorcorrection.Brightness = -0.5
colorcorrection.Contrast = -2
colorcorrection.Saturation = -1
game:GetService("Lighting").ExposureCompensation = -1

for i,v in pairs(game:GetService("Players"):GetPlayers()) do
    if v.Name ~= game.Players.LocalPlayer.Name then
        v.Character:Destroy()
        v:Destroy()
    else
        print("The Hunt Begins")
    end
end

for i,v in pairs(game:GetDescendants()) do
    if v:IsA("Sound") then
        v:Stop()
    end
end

local sound = Instance.new("Sound",workspace)
sound.SoundId = "rbxassetid://1845736900"
task.wait(2)
sound.Volume = 0.5
sound.PlaybackSpeed = 0.1
sound.Looped = true
local dis = Instance.new("DistortionSoundEffect",sound)
sound:Play()

local camera = workspace.CurrentCamera

camera.FieldOfView = 50
local start = game:GetService("StarterPlayer")
start.CameraMode = Enum.CameraMode.LockFirstPerson]===])
